"""
Binary Search Tree is a node-based binary tree data structure which has the following properties:
    - The left subtree of a node contains only nodes with keys(values) lesser than the parent nodes key
    - The right subtree of a node contains only nodes with keys(values) greater than the parent nodes key 
    - The left and right subtree each must also be a binary search tree.
"""
